# Secrets — NEVER commit. Keys live here locally, in the container they are injected as env vars.
.env
.env.*
!.env.example

# Dependencies / build output
node_modules/
dist/
build/
coverage/
*.log
# Baseline demo log is committed as Phase-2 evidence.
!baseline/runs.log

# Runtime data: traces + eval runs (may contain redacted question previews)
server/data/
data/

# Editor / OS
.DS_Store
.vscode/
.idea/
